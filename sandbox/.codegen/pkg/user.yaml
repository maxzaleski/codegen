name: user

models:
  - name: User
    extends: internal.PublicEntity
    props:
      - name: Name
        type: string
        desription: The user's name
        addons:
          - key: tag
            value: 
              - key: gorm
                value: type:varchar(102);not null
              - key: json
                value: name,omitempty
      - name: SomePrivateProp
        type: string
        scope: private
        addons:
          - key: tags
            value: 
              - key: gorm
                value: type:varchar(100);not null
              - key: json
                value: name,omitempty
    methods:
      - name: TableName
        params:
          - name: _
            type: \*gorm.DB
        returns:
          - type: string

interface:
  methods:
    - name: Create
      description: Create creates a new user.
      params:
        - name: u
          type: User
    - name: Get
      description: Get retrieves an existing user.
      params:
        - name: uID
          type: string 
      returns:
        - type: User
